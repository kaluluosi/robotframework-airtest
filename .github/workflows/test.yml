name: Test-测试


on:
    push:
        paths-ignore:
            - ".github/**"
        branches-ignore:
            - gh-pages
    pull_request:
        paths-ignore:
            - ".github/**"
    
    workflow_dispatch:
    workflow_call:


jobs:
    test-android:
        runs-on: ubuntu-latest
        steps:
            - name: Env Setup
              uses: ./.github/workflows/actions/env_setup
              
